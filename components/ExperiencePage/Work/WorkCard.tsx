import React from "react"
import { workItem } from "../../../Utils/data/WorkData"

interface workCard{
    work: workItem
}

const WorkCard = ( prop: workCard ) => {
    return (
        <div>

        </div>
    )
}

export default WorkCard